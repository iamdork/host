---
- hosts: test
  gather_facts: no
  pre_tasks:
  - raw: yum install python -y
    ignore_errors: yes
  - raw: apt-get install python -y
    ignore_errors: yes
  - setup:
  roles:
    - role: ansible-dork-host
      users: "{{playbook_dir}}/users"
